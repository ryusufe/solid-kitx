import { BackgroundGridProps } from ".";
import type { StateType } from "./state";
import type { HelperType } from "./helper";

export type LogicType = {};

export const BackgroundGridLogic = (
        state: StateType,
        props: BackgroundGridProps,
        helper?: HelperType,
): LogicType => {
        return {};
};
